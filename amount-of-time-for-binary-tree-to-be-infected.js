var amountOfTime = function (root, start) {
    let graph = {}

    let inOrder = (node) => {
        if (!node) return

        inOrder(node.left)
        if (!graph[node.val]) graph[node.val] = []

        if (node.left) {
   }

    bfs()


    return distance

};
